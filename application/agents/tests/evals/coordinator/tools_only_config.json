{
  "criteria": {
    "tool_trajectory_avg_score": 0.4
  },
  "user_simulator_config": {
    "max_allowed_invocations": 50
  },
  "timeout": 600,
  "comment": "Threshold 0.6 allows for reasonable variation in sub-agent tool calls while ensuring main routing tools are called"
}
